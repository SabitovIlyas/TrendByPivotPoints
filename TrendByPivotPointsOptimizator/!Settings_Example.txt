PositionSide:Long
TimeFrames:60min
Strategy:MeanReversion
Seed:42
PopulationSize:100
Generations:300
CrossoverRate:0.85
MutationRate:0.10
Patience:50
TournamentSize:4
MinDiversity:0.1
EliteFraction:0.2
Threads:0
SaveCheckpoint:1
BackwardDays:1460
ForwardDays:1460
ForwardPeriodsCount:10
ShiftWindowDays:30
TrimHistory:1
Equity:100000
RiskValuePrcnt:2
SecuritiesFile:C:\Данные\!Securities_Si.txt
Range:maPeriod:50:250:1
Range:atrMultiplier:0.5:3:0.5

# Пример файла настроек оптимизатора. Строки, начинающиеся не с ключа, игнорируются.
#
# PositionSide: Long или Short (сторона торговли)
# TimeFrames: 01min, 05min, 15min, 30min, 60min или 1d
# Strategy: имя стратегии для универсального оптимизатора:
#     MeanReversion      - возврат к среднему (RSI + SMA-фильтр + ATR-стоп)
#     DonchianUniversal  - прорыв канала Дончиана через универсальный путь
#   Если строка Strategy отсутствует - работает прежний оптимизатор Дончиана.
# Seed: сид генератора случайных чисел; убрать строку - случайный запуск.
# PopulationSize/Generations/CrossoverRate/MutationRate/Patience: параметры
#   генетического алгоритма (умолчания: 100/300/0.85/0.10/50).
# TournamentSize: сколько случайных особей соревнуются за право стать
#   родителем (4). MinDiversity: порог разнообразия популяции, ниже которого
#   оптимизация останавливается (0.1).
# EliteFraction: доля лучших особей, переходящих в следующее поколение без
#   изменений (0.2 - это 20 особей при популяции 100). Элита не пересчитывается,
#   поэтому чем больше доля, тем быстрее поколение, но тем быстрее вырождается
#   популяция. Переносится хотя бы одна особь.
# Threads: сколько потоков считают хромосомы поколения; 0 - по числу ядер (по
#   умолчанию), 1 - последовательно. На результат не влияет: прогоны хромосом
#   независимы, а случайные числа берутся вне расчёта. Ставьте 1 или 2, если
#   нужно оставить машину отзывчивой.
# SaveCheckpoint: 1 - сохранять состояние прогона после каждого поколения (по
#   умолчанию), 0 - не сохранять. Если прогон прервётся (отключат питание,
#   закроете окно), следующий запуск с теми же настройками продолжит с
#   последнего сохранённого поколения, а не начнёт заново. Чек-поинт привязан
#   к настройкам: поменяли любой параметр поиска или окна - прогон начнётся
#   сначала. После успешного завершения файл удаляется.
# CheckpointFile: путь к файлу чек-поинта. Без этой строки он лежит рядом со
#   сводным отчётом под именем вида Si_Long_MeanReversion_checkpoint.txt.
# BackwardDays/ForwardDays: окна бэктеста и форвардного теста в днях (1460/1460).
# ForwardPeriodsCount: количество форвардных периодов (10).
# ShiftWindowDays: смещение окна между периодами в днях (30).
# TrimHistory: 1 - отрезать историю старше самого раннего окна тестирования (по
#   умолчанию), 0 - грузить файл котировок целиком. Обрезка не влияет на
#   результат: отрезаются только бары, не попадающие ни в один прогон, — файл с
#   котировками можно подавать как есть, вручную обрезать его не нужно.
# Equity: стартовый капитал (100000). RiskValuePrcnt: риск на сделку, % (2).
# SecuritiesFile: путь к файлу с инструментами; если задан - оптимизатор не
#   спрашивает его диалогом (запуск без интерфейса: передайте путь к этому
#   файлу настроек аргументом командной строки оптимизатора).
# SeedGenesFile: путь к JSON-файлу затравочной хромосомы. Строку можно убрать:
#   без затравки стартовая популяция просто будет полностью случайной.
# LogFile: файл журнала прогона. Без этой строки журнал пишется в рабочую папку
#   оптимизатора под именем вида Optimizator_2026-07-30_19-45-00.log.
# Range:имя:мин:макс:шаг - переопределение диапазона поиска параметра стратегии
#   (по одному параметру на строку; не указанные параметры ищутся в диапазонах
#   по умолчанию). У MeanReversion пороги RSI зависят от стороны: лонг - вход
#   [5;50]/выход [50;95], шорт - вход [50;95]/выход [5;50].
